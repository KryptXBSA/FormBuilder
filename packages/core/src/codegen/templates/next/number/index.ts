import { dualSlider } from './dual-slider';
import { number } from './number';
import { phoneNumber } from './phone-number';
import { slider } from './slider';

export const numberTemplates = {
    dualSlider,
    number,
    phoneNumber,
    slider,
} as const;